import 'package:flutter/material.dart';

class RequestsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Book Issue Requests")),
      body: Center(child: Text("Approve/Reject Requests Page")),
    );
  }
}
